[pytest]
pythonpath = .
# Environment variables injected for every test run.
# JWT_SECRET must match what conftest.py sets via os.environ.setdefault.
env =
    JWT_SECRET=test-only-secret-do-not-use-in-production